; Similar quality at roughly half the size, slower to encode.
; Use with RenderVideoPreset=h265 in Render.ini

[Preset]
Args=-c:v libx265 -crf 20 -preset slow -pix_fmt yuv420p -sws_flags +accurate_rnd+full_chroma_int -colorspace bt709 -color_primaries bt709 -color_trc bt709 -color_range tv
Ext=mp4
